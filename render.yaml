services:
  - type: web
    name: rethink-einkauf
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --timeout 45 server:app
    healthCheckPath: /health
    autoDeploy: true
